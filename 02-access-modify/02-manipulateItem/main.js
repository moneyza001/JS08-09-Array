let styles = ['Jazz', 'Blues'];

styles[styles.length('Rock-n-Roll')];
styles[1] = 'Classic';
delete styles[0];
styles.length() // 3 เพราะคำสั่ง delete เป็นการลบค่าใน array แต่ไม่ได้ลบตำแหน่งใน array 